#ifndef UTILS_HPP
#define UTILS_HPP

#include <string>
#include <vector>
#include "Reply.hpp"

using namespace std;

// // join
// string	join(vector<string> vec);

// split
vector<string>	split(const string& str, const string& delimiter);

#endif